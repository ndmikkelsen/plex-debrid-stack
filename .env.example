# =============================================================================
# Environment Variables for Plex-Debrid Stack
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit your .env file to version control!
# =============================================================================

# =============================================================================
# USER/GROUP CONFIGURATION
# =============================================================================
# Find your IDs by running: id $(whoami)
# These should match your host user to avoid permission issues
PUID=1000
PGID=1000

# =============================================================================
# TIMEZONE
# =============================================================================
# List of valid timezones: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=America/New_York

# =============================================================================
# PLEX CONFIGURATION
# =============================================================================
# Only required for NEW Plex installations
# Get a claim token from: https://plex.tv/claim
# This token expires after 4 minutes, so generate it right before first run
# Leave empty if Plex is already configured
PLEX_CLAIM=

# =============================================================================
# CLOUDFLARE TUNNEL (Optional - for external access)
# =============================================================================
# Get this from: Cloudflare Zero Trust → Networks → Tunnels → Create Tunnel
# Start with: docker compose --profile tunnel up -d
# Leave empty if not using Cloudflare Tunnel
CLOUDFLARE_TUNNEL_TOKEN=
